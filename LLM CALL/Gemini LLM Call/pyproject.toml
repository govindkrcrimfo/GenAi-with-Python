[project]
name = "gemini-llm-call"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "govindkrcrimfo", email = "govind.krcrimfo555@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "google-genai>=2.24.0",
]

[project.scripts]
gemini-llm-call = "gemini_llm_call:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
